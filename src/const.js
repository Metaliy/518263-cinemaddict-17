
const SortType = {
  DEFAULT: 'default',
  DATE: 'date',
  RATING: 'rating',
};

const FILM_COUNT_PER_STEP = 5;
const ADDITIONAL_FILM_COUNT = 2;

export {SortType, FILM_COUNT_PER_STEP, ADDITIONAL_FILM_COUNT};
